/**
 * Type definitions for the nuxt-lite module.
 * Import this in your nuxt.config.ts for autocomplete and type checking.
 *
 * @example
 * ```ts
 * import type { NuxtLiteOptions } from '~~/nuxt-lite/src'
 *
 * const nuxtLite: NuxtLiteOptions = {
 *   optimizeCss: 'inline',
 *   stripAttributes: ['data-v-', '__vue_ssr__', 'data-server-rendered'],
 * }
 * ```
 */
export type { ModuleOptions as NuxtLiteOptions } from './types'
